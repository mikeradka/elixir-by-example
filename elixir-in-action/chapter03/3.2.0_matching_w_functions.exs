"
MATCHING WITH FUNCTIONS

The pattern-matching mechanism is used in the specification of function
arguments. Recall the basic function definition:

  def my_function(arg1, arg2) do
    ...
  end

The argument specifiers arg1 and arg2 are patterns, and you can use standard
matching techniques.
"
|> IO.puts()
