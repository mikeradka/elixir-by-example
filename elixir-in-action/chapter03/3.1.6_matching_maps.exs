"
MATCHING MAPS

To match a map, you can use the following syntax:

  iex(1)> %{name: name, age: age} = %{name: \"Bob\", age: 25}
  %{name: \"Bob\", age: 25}
  iex(2)> name
  \"Bob\"
  iex(3)> age
  25
"
|> IO.puts()
